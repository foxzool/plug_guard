defmodule PlugGuard.OAuth.Authorization.Token.Test do
  use ExUnit.Case, async: true
  use Plug.Test

  test "return error when grant_type is invalid" do
  #    assert_raise ArgumentError, "invalid argument grant_type", fn ->
    #    conn = conn(:get, "/")
    #PlugGuard.OAuth.Authorization.Token.call(conn, [])
      #end

  #assert_raise ArgumentError, "invalid argument grant_type", fn ->
    #    conn = conn(:get, "/", %{"grant_type" => "password"})
    #  PlugGuard.OAuth.Authorization.Token.call(conn, [])
    #end
  end
end
